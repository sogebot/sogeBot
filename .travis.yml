language: node_js
services: mongodb
node_js:
  - "8"
  - "9"
before_install:
- npm install -g mocha standard

jobs:
  include:
    - stage: STANDARD Compliance tests
    - script: npm run standard
    - stage: NEDB
    - script: npm run test:config:nedb && npm run test
    - stage: MONGODB
    - script: npm run test:config:mongodb && npm run test
